{
  "name": "context-seal",
  "version": "0.1.0",
  "private": false,
  "description": "Private AI action receipts for provenance, policy, and tamper-evident tool decisions.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "lint": "node --check server.mjs && node --check src/policy.mjs && node --check public/app.js && node --check public/graph.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
